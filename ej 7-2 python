import time

hora = time.strftime('%H') 
minutos = time.strftime('%M') 

if int(hora) >= 19:
	print ("Es hora de ir a casa") 
else:
	print ("Quedan "+str(18- int(hora))+" horas y "+str(59-int(minutos))+" minutos para ir a casa")
